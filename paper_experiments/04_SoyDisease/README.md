# SoyDisease

SoyDisease is treated as a separate crop/dataset evaluation, not as SoyCultivar200.

Include only the SoyDisease notebooks/results that correspond to the experiments identified in the first draft. Preserve their dataset definition and class selection even if a source notebook has an execution error at its final cell; the execution status must be reported rather than silently changing the experiment.
